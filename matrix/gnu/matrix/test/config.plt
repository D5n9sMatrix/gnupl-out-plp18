#!/usr/bin/gnuplot -persist
# If you are a regular gnuplot user you most probably want to reuse some common settings. 
# I normally avoid it on this blog to have easy scripts that run as standalone files, but during 
# my work I use a lot of small config files.
